{
  "name": "python_exec",
  "description": "Run Python code",
  "functions": [
    {
      "name": "run_python",
      "description": "Execute Python code",
      "parameters": {
        "type": "object",
        "properties": {
          "code": { "type": "string" },
          "safe_mode": { "type": "boolean", "default": true }
        },
        "required": ["code"]
      }
    }
  ]
}
